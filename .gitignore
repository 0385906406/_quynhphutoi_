# Cấu hình agent / công cụ cục bộ
.claude/
.tmp-check.*

# Phụ thuộc & build (web/ cũng có .gitignore riêng)
node_modules/
.next/
out/
build/

# Biến môi trường & bí mật — TUYỆT ĐỐI không commit
.env
.env.*

# Khác
.DS_Store
*.tsbuildinfo
.vercel

# Tài liệu trình chiếu / soạn thảo & nội dung giải nén — không đưa vào repo
*.pptx
*.docx
_pptx_extract/
_slides_text.txt
_docx_build/
