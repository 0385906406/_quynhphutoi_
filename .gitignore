# Cấu hình agent / công cụ cục bộ
.claude/
.tmp-check.*

# Phụ thuộc & build (web/ cũng có .gitignore riêng)
node_modules/
.next/
out/
build/

# Biến môi trường & bí mật — TUYỆT ĐỐI không commit
.env
.env.*

# Khác
.DS_Store
*.tsbuildinfo
.vercel

# Tài liệu trình chiếu & nội dung giải nén — không đưa vào repo
*.pptx
_pptx_extract/
_slides_text.txt
